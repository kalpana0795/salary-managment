'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#780c1e',
    },
    secondary: {
      main: '#e58c95',
    },
  },
});

export default theme;
