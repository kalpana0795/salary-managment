'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#780c1e',
    },
    secondary: {
      main: '#2e7d32',
    },
  },
});

export default theme;
